# 🗄️ База данных приложения

## Структура базы данных

### Таблица `users`
| Поле | Тип | Описание |
|------|-----|----------|
| user_id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID пользователя |
| first_name | TEXT NOT NULL | Имя пользователя |
| email | TEXT UNIQUE NOT NULL | Email (уникальный) |
| pass | TEXT NOT NULL | Пароль (в открытом виде - для демо) |
| created_at | DATETIME DEFAULT CURRENT_TIMESTAMP | Дата регистрации |

### Таблица `todos`
| Поле | Тип | Описание |
|------|-----|----------|
| todo_id | INTEGER PRIMARY KEY AUTOINCREMENT | Уникальный ID задачи |
| user_id | INTEGER NOT NULL | ID пользователя (внешний ключ) |
| title | TEXT NOT NULL | Заголовок задачи |
| text | TEXT | Описание задачи |
| completed | INTEGER DEFAULT 0 | Статус: 0 - не выполнена, 1 - выполнена |
| created_at | DATETIME DEFAULT CURRENT_TIMESTAMP | Дата создания |

## Демо-данные

При первом запуске автоматически создаются:

### Демо-пользователь:
- **Email:** demo@example.com
- **Пароль:** demo123
- **Имя:** Demo User

### Демо-задачи:
1. "Первая задача" - не выполнена
2. "Выполненная задача" - выполнена
3. "Важная задача" - не выполнена

## Особенности реализации

### На Render.com:
- База данных сохраняется в `/tmp/todolist.db`
- Данные сохраняются между перезапусками сервера
- При деплое нового кода база не сбрасывается
- Файл БД сохраняется до полного удаления сервиса

### Внешние ключи:
```sql
FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
```
### При удалении пользователя автоматически удаляются все его задачи

### Включено с помощью PRAGMA foreign_keys = ON