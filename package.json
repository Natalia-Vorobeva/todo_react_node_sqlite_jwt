{
	"dependencies": {
		"concurrently": "^9.2.1",
		"dotenv": "^17.2.3"
	},
	"scripts": {
		"dev:": "vite",
		"start": "concurrently \"cd ./server && node db.js && npm run server && cd ..\" \"cd ./client && npm run dev\"",
		"build": "npm run build --prefix client",
		"predeploy": "npm run build"
	}
}
